# twitter_support_agent
An AI-based customer support system that classifies customer queries, generates replies using similar historical support conversations, and decides whether to automatically handle the issue or escalate it to a human agent.
